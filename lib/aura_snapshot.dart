import 'package:flutter/material.dart';

class AuraSnapshot extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "Aura Snapshot Feature Coming Soon",
        style: TextStyle(fontSize: 18),
      ),
    );
  }
}
